import React from 'react'

import AppBar from '../../../components/desktop/AppBar'

export default class Restore extends React.Component {
  render() {
    return (
      <AppBar fullWidth withGrid backButton>
        <p>Restore</p>
      </AppBar>
    )
  }
}
