import AuthenticationAdapter from './AuthenticationAdapter'

export default class FingerprintAdapter extends AuthenticationAdapter {
  /**
   * Checks if user is authenticated
   */
  check = () => {}
}
